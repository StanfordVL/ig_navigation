# @package _global_
experiment_name: pointnav
task_type: pointnav

# task
task: point_nav_random
target_dist_min: 1.0
target_dist_max: 10.0
goal_format: polar
task_obs_dim: 4

dist_tol: 1


# rewards
reward_functions:
- search
- potential

# Reward scaling
success_reward_scaling: 10.0
potential_reward_scaling: 1.0
